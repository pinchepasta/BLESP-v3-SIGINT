<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#000000"
    android:padding="8dp">

    <View
        android:id="@+id/viewTopBorder"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#4400FF41"
        app:layout_constraintTop_toTopOf="parent" />

    <FrameLayout
        android:id="@+id/avatarContainer"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/viewTopBorder">

        <View
            android:id="@+id/viewAvatarBg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/status_box_bg_black"
            android:padding="2dp" />

        <TextView
            android:id="@+id/tvAvatarText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="monospace"
            android:text="P"
            android:textColor="#00FF41"
            android:textSize="22sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/viewStatusIndicator"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="1dp"
            android:src="@drawable/ic_online"
            android:visibility="visible" />

        <View
            android:id="@+id/viewUnreadIndicator"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_gravity="top|end"
            android:layout_margin="2dp"
            android:background="@drawable/status_box_bg_pink"
            android:visibility="gone" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvDeviceName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:fontFamily="monospace"
        android:singleLine="true"
        android:text="UNIT_NAME"
        android:textColor="#00FF41"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@+id/tvTime"
        app:layout_constraintStart_toEndOf="@+id/avatarContainer"
        app:layout_constraintTop_toTopOf="@+id/avatarContainer" />

    <TextView
        android:id="@+id/tvTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:fontFamily="monospace"
        android:text="12:34"
        android:textColor="#4400FF41"
        android:textSize="12sp"
        app:layout_constraintEnd_toStartOf="@+id/btnDeleteChat"
        app:layout_constraintTop_toTopOf="@+id/tvDeviceName" />

    <ImageButton
        android:id="@+id/btnDeleteChat"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="4dp"
        android:src="@android:drawable/ic_menu_delete"
        android:tint="#FF00FF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvLastMessage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:fontFamily="monospace"
        android:singleLine="true"
        android:text="Last message snippet..."
        android:textColor="#8800FF41"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/tvDeviceName"
        app:layout_constraintTop_toBottomOf="@+id/tvDeviceName" />

</androidx.constraintlayout.widget.ConstraintLayout>